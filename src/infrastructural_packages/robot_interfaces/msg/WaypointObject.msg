# This message represents our waypoints
# A waypoint will have the actual GPS data of type PointStamped()
# It will also have a string field to denote the type of marker (GPS, intermediate, etc.)
geometry_msgs/PointStamped data
string type
string type_descriptor
